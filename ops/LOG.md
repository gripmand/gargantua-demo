# Operations log

Append-only. Newest last. One entry per session.

---

## 2026-08-30 — opened

Established the commercial thesis and the bar (`ops/PLAYBOOK.md`). Built the
validation landing page at `launch/` — pricing anchored at $29 founding / $49
standard, qualifying fields on the form, `?src=` channel attribution, disclaimer.

Status `PAPER`. Signups 0: the form has no endpoint yet, which is the one blocker
that gates the entire test.

**Next:** human wires `FORM_ENDPOINT`, adds analytics, posts the first methodology
piece. Nothing else can move until the form collects.
